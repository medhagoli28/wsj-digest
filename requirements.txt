# Runtime dependencies. The fetch layer is stdlib-only; the research layer (Stage 2)
# uses the Anthropic SDK for Claude's server-side web_search tool, and OpenAI for
# headline embeddings used by cross-day de-duplication (see dedup.py).
anthropic>=0.40
openai>=1.0
