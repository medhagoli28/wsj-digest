# Generated / intermediate data — regenerated on each run
digest.json
digest_full.json
worklist.json

# Dated digests (digest-<date>.md) ARE committed — the daily workflow writes them.

# Python
__pycache__/
*.pyc
.pytest_cache/
venv/
.venv/

# Local env
.env

# Generated GitHub Pages output (published to the gh-pages branch, not master)
public/

# macOS
.DS_Store
